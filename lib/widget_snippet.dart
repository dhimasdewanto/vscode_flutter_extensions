import 'package:flutter/material.dart';

class WidgetSnippet extends StatefulWidget {
  WidgetSnippet({Key key}) : super(key: key);

  @override
  _WidgetSnippetState createState() => _WidgetSnippetState();
}

class _WidgetSnippetState extends State<WidgetSnippet> {
  @override
  void didChangeDependencies() {
    super.didChangeDependencies();
  }

  @override
  void didUpdateWidget(WidgetSnippet oldWidget) {
    super.didUpdateWidget(oldWidget);
  }

  @override
  void dispose() {
    super.dispose();
  }

  @override
  void initState() {
    super.initState();
  }

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
